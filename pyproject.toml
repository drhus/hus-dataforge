[project]
name = "hus-dataforge"
version = "0.1.0"
description = "Universal scraping → cleaning → packaging pipeline for LLM training datasets"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
authors = [
    { name = "Husam Abboud", email = "husam@drhus.com" }
]

dependencies = [
    "scrapy>=2.11",
    "playwright>=1.40",
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "typer>=0.12",
    "redis>=5.0",
    "rq>=1.16",
    "datasets>=2.19",
    "ftfy>=6.2",
    "langdetect>=1.0",
    "datasketch>=1.6",
    "huggingface-hub>=0.23",
    "pyyaml>=6.0",
    "rich>=13.7",
    "sqlalchemy>=2.0",
    "pydantic>=2.7",
    "httpx>=0.27",
    "selectolax>=0.3.21",
    "telethon>=1.36",
    "pyarrow>=15.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "httpx>=0.27",
    "ruff>=0.5",
]

[project.scripts]
dataforge = "packages.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["packages"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]

[tool.pytest.ini_options]
testpaths = ["tests"]
